diff --git a/types.ts b/types.ts
index 2cecd5d..78f4d4e 100644
--- a/types.ts
+++ b/types.ts
@@ -1,4 +1,4 @@
-﻿
+﻿﻿
 export type Module = 'dashboard' | 'recebimento' | 'movimentacoes' | 'auditoria_geral' | 'estoque' | 'expedicao' | 'inventario_ciclico' | 'compras' | 'gestao_compras' | 'cadastro' | 'relatorios' | 'configuracoes';
 
 export const ALL_MODULES: { id: Module; label: string }[] = [
@@ -81,7 +81,7 @@ export interface Movement {
   location: string;
   reason: string;
   orderId?: string;
-  warehouseId: string; // NOVO: ArmazÃ©m onde ocorreu a movimentaÃ§Ã£o
+  warehouseId: string; // NOVO: Armazém onde ocorreu a movimentação
 }
 
 export interface InventoryItem {
@@ -101,7 +101,7 @@ export interface InventoryItem {
   safetyStock: number;
   abcCategory?: 'A' | 'B' | 'C';
   lastCountedAt?: string;
-  warehouseId: string; // NOVO: ArmazÃ©m onde o item estÃ¡ localizado
+  warehouseId: string; // NOVO: Armazém onde o item está localizado
 }
 
 export interface Quote {
@@ -157,13 +157,18 @@ export interface PurchaseOrder {
   approvalHistory?: ApprovalRecord[];
   plate?: string;
   costCenter?: string;
-  warehouseId: string; // NOVO: ArmazÃ©m de destino do pedido
+  warehouseId: string; // NOVO: Armazém de destino do pedido
 }
 
 export interface Vendor {
   id: string;
-  name: string;
+  idFornecedor?: number;
+  razaoSocial: string;
+  nomeFantasia?: string;
   cnpj: string;
+  telefone?: string;
+  // Campos legados mantidos para compatibilidade entre módulos.
+  name: string;
   category: string;
   contact: string;
   email: string;
@@ -191,6 +196,7 @@ export interface User {
   password?: string;
   allowedWarehouses: string[]; // NOVO: Armazéns que o usuário pode acessar
   hasWorkshopAccess?: boolean; // NOVO: Acesso ao módulo Oficina
+  hasFleetAccess?: boolean; // NOVO: Acesso ao módulo Gestão de Frota
 }
 
 export interface AppNotification {
@@ -211,7 +217,7 @@ export interface CyclicBatch {
   accuracyRate?: number;
   totalItems: number;
   divergentItems: number;
-  warehouseId: string; // NOVO: ArmazÃ©m onde o inventÃ¡rio estÃ¡ sendo realizado
+  warehouseId: string; // NOVO: Armazém onde o inventário está sendo realizado
 }
 
 export interface CyclicCount {
@@ -235,7 +241,7 @@ export interface CostCenter {
 }
 
 // ===== SISTEMA DE MÓDULOS =====
-export type SystemModule = 'warehouse' | 'workshop';
+export type SystemModule = 'warehouse' | 'workshop' | 'fleet';
 
 // ===== MÓDULO OFICINA =====
 export type WorkOrderStatus = 'aguardando' | 'em_execucao' | 'aguardando_pecas' | 'finalizada' | 'cancelada';
@@ -249,7 +255,7 @@ export interface WorkOrder {
   status: WorkOrderStatus;
   type: WorkOrderType;
   priority: 'baixa' | 'normal' | 'alta' | 'urgente';
-  mechanicId?: string;
+  mechanicId?: string; // Mantido para compatibilidade (Lead Mechanic)
   mechanicName?: string;
   description: string;
   services: ServiceItem[];
@@ -267,6 +273,13 @@ export interface WorkOrder {
   };
   createdBy: string;
   warehouseId: string;
+  
+  // New fields for improvements
+  totalSeconds?: number; // Tempo total acumulado em segundos
+  lastStatusChange?: string; // Timestamp da última mudança de status
+  isTimerActive?: boolean; // Se o contador está rodando
+  lockedBy?: string; // ID do usuário editando
+  lockedAt?: string; // Timestamp do início da edição
 }
 
 export interface ServiceItem {
@@ -276,6 +289,21 @@ export interface ServiceItem {
   estimatedHours: number;
   actualHours?: number;
   completed: boolean;
+  
+  // New fields
+  mechanicId?: string; // Mecânico responsável por este serviço
+  mechanicName?: string;
+  actualHours?: number;
+}
+
+export interface TimeLog {
+  id: string;
+  workOrderId: string;
+  previousStatus: WorkOrderStatus;
+  newStatus: WorkOrderStatus;
+  timestamp: string;
+  userId: string;
+  durationSeconds?: number; // Tempo decorrido no status anterior
 }
 
 export interface PartRequest {
@@ -427,6 +455,13 @@ export const WORK_ORDER_STATUS_LABELS: Record<WorkOrderStatus, string> = {
   cancelada: 'Cancelada'
 };
 
+export const WORK_ORDER_PRIORITY_LABELS: Record<WorkOrder['priority'], string> = {
+  baixa: 'Baixa',
+  normal: 'Normal',
+  alta: 'Alta',
+  urgente: 'Urgente'
+};
+
 export const WORK_ORDER_TYPE_LABELS: Record<WorkOrderType, string> = {
   preventiva: 'Preventiva',
   corretiva: 'Corretiva',
