diff --git a/api-client.ts b/api-client.ts
index 5de6c13..7538dd2 100644
--- a/api-client.ts
+++ b/api-client.ts
@@ -5,7 +5,9 @@ const trimSlash = (value: string) => value.replace(/\/+$/, '');
 const resolveBaseUrl = () => {
   const envUrl = String(import.meta.env.VITE_API_URL || '').trim();
   if (!envUrl) {
-    return import.meta.env.DEV ? 'http://127.0.0.1:3001' : '/api';
+    // In development, we use the proxy configured in vite.config.ts
+    // This allows the frontend to work correctly in both local and cloud IDE environments
+    return import.meta.env.DEV ? '/api' : '/api';
   }
   if (envUrl.startsWith('http://') || envUrl.startsWith('https://')) {
     return trimSlash(envUrl);
