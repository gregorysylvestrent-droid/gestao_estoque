diff --git a/pages/workshop/WorkshopDashboard.tsx b/pages/workshop/WorkshopDashboard.tsx
index 4f15eed..2af3755 100644
--- a/pages/workshop/WorkshopDashboard.tsx
+++ b/pages/workshop/WorkshopDashboard.tsx
@@ -1,4 +1,4 @@
-import React, { useState, useMemo } from 'react';
+﻿import React, { useState, useMemo } from 'react';
 import { WorkshopKPIs, WorkOrder, Mechanic, WORK_ORDER_STATUS_LABELS, WORK_ORDER_TYPE_LABELS } from '../../types';
 import { formatCurrency } from '../../utils/format';
 
@@ -155,7 +155,7 @@ export const WorkshopDashboard: React.FC<WorkshopDashboardProps> = ({
             <div className="flex items-center gap-2">
               {kpi.trend !== null && (
                 <span className={`text-sm font-medium ${kpi.trend >= 0 ? 'text-emerald-500' : 'text-red-500'}`}>
-                  {kpi.trend >= 0 ? '↑' : '↓'} {Math.abs(kpi.trend)}%
+                  {kpi.trend >= 0 ? 'â†‘' : 'â†“'} {Math.abs(kpi.trend)}%
                 </span>
               )}
               <span className="text-sm text-slate-500 dark:text-slate-400">
@@ -424,3 +424,4 @@ export const WorkshopDashboard: React.FC<WorkshopDashboardProps> = ({
     </div>
   );
 };
+
