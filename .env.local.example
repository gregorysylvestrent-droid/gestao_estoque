# Frontend local (Vite)
VITE_API_URL=/api
VITE_API_TIMEOUT_MS=15000
VITE_API_GET_RETRIES=2

# Docker compose local DB (used by docker compose + transfer-data.sh)
LOCAL_DB_HOST=127.0.0.1
LOCAL_DB_PORT=5432
LOCAL_DB_NAME=logiwms_db
LOCAL_DB_USER=postgres
LOCAL_DB_PASSWORD=postgrespassword

# RDS source (used by transfer-data.sh)
RDS_HOST=seu-rds-endpoint.amazonaws.com
RDS_PORT=5432
RDS_DB=logiwms_db
RDS_USER=admin_logiwms
RDS_PASSWORD=replace-with-rds-password
