PORT=3001
NODE_ENV=development

# PostgreSQL local (docker compose / serviço local)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=logiwms_db
DB_USER=postgres
DB_PASSWORD=postgrespassword
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=false
DB_HEALTHCHECK_INTERVAL_MS=10000

# Segurança
JWT_SECRET=troque-por-segredo-forte-32-caracteres-no-minimo
JWT_EXPIRES_IN=8h

# Permita o frontend local + frontend publicado no EC2
# Ajuste o IP/DOMINIO conforme seu ambiente
CORS_ORIGIN=http://localhost:3000,http://3.83.164.82

FLEET_SYNC_ALLOWED_HOSTS=cubogpm-frota.nortesistech.com
